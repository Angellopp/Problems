#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    int tt;
    cin >> tt;
    while (tt--) {
        int n, m;
        cin >> n >> m;
        cout << n * (m / 2) << " \n";
    }
    return 0;
}