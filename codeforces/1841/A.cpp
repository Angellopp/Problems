#include <bits/stdc++.h>

using namespace std;

int main () {
    int tt; cin >> tt;
    int aux;
    while (tt--) {
        cin >> aux;
        if(aux >= 5) {
            cout << "Alice\n";
        }
        else 
            cout << "Bob\n";
    }

  return (0);
}